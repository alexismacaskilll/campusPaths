# Tests that a graph can have a self-edge, a node who has itself as one of its children

# Create a graph
CreateGraph graph1

# Add a node to the graph
AddNode graph1 n1

# Add a self edge to the graph
AddEdge graph1 n1 n1 label

# Print the outgoing edges from n1
ListChildren graph1 n1